# health_mobile

Flutter 客户端（手机、Pad、Web）。

## 初始化

当前环境未提供 Flutter SDK，请在本地执行：

```bash
cd apps
flutter create mobile --platforms=android,ios,web
```

再将已有 `lib/main.dart` 与项目代码合并。

## 启动

```bash
flutter pub get
flutter run -d chrome
```
