# health

健康管理应用 Monorepo，包含：

- `apps/mobile`：Flutter 客户端（Android/iOS/Web）
- `apps/backend`：NestJS 后端服务
- `packages/shared`：共享类型定义
- `docs`：架构与开发文档

## 快速开始

```bash
npm run backend:lint
npm run backend:build
npm run backend:test
```

移动端请先安装 Flutter SDK 后参考 `apps/mobile/README.md`。
